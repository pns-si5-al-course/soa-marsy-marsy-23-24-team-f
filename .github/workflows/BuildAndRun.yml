name: Build and run

on:
    pull_request:
        branches: ["main"]

jobs:
    build-and-run:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Build
              run: cd rocket-department&&./build.sh

            - name: Run
              run: docker run -dp 3001:3001 rocket-status

        
            
