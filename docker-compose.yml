version: "3"

services:
  rocket-dept:
      image: rocket-dept
      build:
        context: ./rocket-department/rocket-department/
        dockerfile: Dockerfile.production
      ports:
        - "3001:3001"
      
